function one(){
    const name="Basava"
    function two(){
        age:23
        console.log(name);
        
    }
        
    // console.log(age) gives an error bcoz declared outside the func
    two()// if we dont call for function it willl never execute
    
}
one()